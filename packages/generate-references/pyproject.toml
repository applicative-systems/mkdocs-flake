[project]
name = "generate-references"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alexander Foremny", email = "aforemny@posteo.de" }
]
requires-python = ">=3.13"
dependencies = [
    "mkdocs-gen-files>=0.6.1",
    "tree-sitter>=0.25.2",
    "tree-sitter-nix>=0.1.0",
]

[project.scripts]
generate-references = "generate_references:main"

[build-system]
requires = ["uv_build>=0.11.4,<0.12.0"]
build-backend = "uv_build"
